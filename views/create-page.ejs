<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Page - Learning Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="font-sans bg-gray-100">
    <%- include('flash-messages.ejs') %>

    <div class="container mx-auto mt-8 p-4 bg-white rounded-md shadow-md">
        <h2 class="text-3xl font-bold mb-6 text-center">Create New Page</h2>
        <form action="/add-page" method="POST">

            <div class="mb-4">
                <label for="pageNo" class="block text-gray-700 font-semibold mb-2">Page Number</label>
                <input type="number" id="pageNo" name="pageNo" class="w-full p-2 border rounded-md" required>
            </div>


            <div class="mb-4">
                <label for="pageTitle" class="block text-gray-700 font-semibold mb-2">Page Title</label>
                <input type="text" id="pageTitle" name="pageTitle" class="w-full p-2 border rounded-md" required>
            </div>


            <div class="mb-4">
                <label for="pageContent" class="block text-gray-700 font-semibold mb-2">Page Content</label>
                <textarea id="pageContent" name="pageContent" rows="4" class="w-full p-2 border rounded-md"
                    required></textarea>
            </div>


            <input type="hidden" id="moduleId" name="moduleId" value="<%= sendId.id %>">


            <div class="text-center">
                <button type="submit"
                    class="bg-blue-500 text-white px-6 py-3 rounded-full font-bold hover:bg-blue-600">Create Page</button>
            </div>
        </form>

		<table class="w-full">
			<thead>
				<tr>
					<th class="py-2 px-4 bg-gray-200">Page No</th>
					<th class="py-2 px-4 bg-gray-200">Page Name</th>
					<th class="py-2 px-4 bg-gray-200"></th>
				</tr>
			</thead>
			<tbody>
				<% for (var page of pages) { %>
					<tr>
						<td class="py-2 px-4 border">
							<%= page.pageNo %>
						</td>
						<td class="py-2 px-4 border">
							<%= page.title %>
						</td>

						<td class="py-2 px-4 border">
							<a class="underline" href="/view-page/<%= page.id %>">View Page</a>
						</td>
					</tr>
					<% } %>
			</tbody>
		</table>
    </div>

</body>

</html>
