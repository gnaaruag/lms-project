<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Chapter - Learning Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>

<body class="font-sans bg-gray-100">
    <%- include('flash-messages.ejs') %>

        <div class="flex justify-center mb-4 mt-4">
            <a class="bg-black text-white px-4 py-2 mr-2  hover:bg-gray-900" href="/dashboard"><i
                    class="fa-solid fa-table-columns" style="color: #ffffff;"></i> Go back to dashboard</a>
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 15 3 9m0 0 6-6M3 9h12a6 6 0 0 1 0 12h-3" />
                      </svg>
                      </a>
        </div>

        <div class="container mx-auto mt-8 p-4 bg-white rounded-md shadow-md">
            <h2 class="text-3xl font-bold mb-6 text-center">Create New Chapter</h2>
            <form action="/add-chapter" method="POST">

                <div class="mb-4">
                    <label for="chapterNumber" class="block text-gray-700 font-semibold mb-2">Chapter Number</label>
                    <input type="number" id="chapterNumber" name="chapterNumber" class="w-full p-2 border rounded-md"
                        required>
                </div>


                <div class="mb-4">
                    <label for="chapterName" class="block text-gray-700 font-semibold mb-2">Chapter Name</label>
                    <input type="text" id="chapterName" name="chapterName" class="w-full p-2 border rounded-md"
                        required>
                </div>


                <input type="hidden" id="courseId" name="courseId" value="<%= sendId.id %>">


                <div class="text-center">
                    <button type="submit" class="bg-black text-white px-6 py-3  font-bold hover:bg-blue-600">Create
                        Chapter</button>
                </div>
            </form>
        </div>

        <table class="flex-col items-center mx-auto mt-4">
            <thead>
                <tr>
                    <th class="py-2 px-4 bg-gray-200">Chapter No</th>
                    <th class="py-2 px-4 bg-gray-200">Chapter Name</th>
                </tr>
            </thead>
            <tbody>
                <% for (var chapter of chapters) { %>
                    <tr>
                        <td class="py-2 px-4 border">
                            <%= chapter.chapterNo %>
                        </td>
                        <td class="py-2 px-4 border">
                            <%= chapter.title %>
                        </td>


                    </tr>
                    <% } %>
            </tbody>
        </table>

</body>

</html>